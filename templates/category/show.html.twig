{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
<!-- <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> -->

<div class="example-wrapper">
    <h1>Detalle de la categoría</h1>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">ID: {{ category.id }}</h5>
            <h5 class="card-title">Código: {{ category.code }}</h5>
            <h5 class="card-title">Nombre: {{ category.name }}</h5>
            <h5 class="card-title">Descripción: {{ category.description }}</h5>
            <h5 class="card-title">Estado: {{ category.status }}</h5>
        </div>
    </div>
        
    <div class="text-center mt-2">
        <a class="btn btn-secondary" href="{{ path('category') }}">&#8592; Listar categorias</a>
    </div>

</div>
{% endblock %}
